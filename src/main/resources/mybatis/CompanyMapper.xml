<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cdkj.gchf.dao.ICompanyDAO">
	<resultMap id="company_DB_Result" type="company">
		<result column="code" property="code" />
		<result column="name" property="name" />
		<result column="create_datetime" property="createDatetime" />
	</resultMap>
	<sql id="where_condition">
		<trim prefix="WHERE" prefixOverrides="AND | OR">
			<if test="code != null and code != '' ">
				AND t.code = #{code}
			</if>
			<if test="keyword != null and keyword != '' ">
				AND t.name like "%"#{keyword}"%"
			</if>
		</trim>
	</sql>
	<select id="select_company" parameterType="company" resultMap="company_DB_Result">
		SELECT
		t.code
		,t.name
		,t.create_datetime
		FROM
		thf_company t
		<include refid="where_condition" />
		<trim prefix="ORDER BY ">
			<if test="order != null and order != '' ">
				t.${order}
			</if>
		</trim>
	</select>

	<select id="select_company_count" parameterType="company"
		resultType="java.lang.Long">
		SELECT count(1) FROM thf_company t
		<include refid="where_condition" />
	</select>

	<insert id="insert_company" parameterType="company">
		INSERT INTO
		thf_company(
		code
		,name
		,create_datetime
		)
		VALUES(
		#{code}
		,#{name}
		,#{createDatetime}
		)
	</insert>

	<update id="update_company" parameterType="company">
		UPDATE thf_company
		SET
		name = #{name}
		WHERE code = #{code}
	</update>

	<delete id="delete_company" parameterType="company">
		DELETE  FROM thf_company 
		WHERE code = #{code}
	</delete>


</mapper>